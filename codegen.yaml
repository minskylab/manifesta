overwrite: true
schema: "http://174.138.47.0/graphql"
documents: "graphql/**/*.graphql"
generates:
    integration/graphql.tsx:
        plugins:
            - "typescript"
            - "typescript-operations"
            - "typescript-urql"
        config:
            withComponent: true
            withHooks: true